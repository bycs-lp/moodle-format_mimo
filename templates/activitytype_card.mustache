{{!
    @template format_mimo/activitytype_card

    Activity type card partial template.

    Context variables required for this template:
    * activitytype - Activity module type (e.g., 'assign', 'quiz')
    * label - Activity type label (e.g., 'Assignment')
    * description - Activity type description
    * icon - Icon HTML
    * purpose - Purpose CSS class (e.g., 'assessment', 'administration')
    * type - Type string (e.g., 'Activity type')
    * tagname - Optional tag name
    * tagcolor - Optional tag color (hex format)

    Example context (json):
    {
        "activitytype": "assign",
        "label": "Assignment",
        "description": "Create an assignment...",
        "icon": "<img src='...' alt='Assignment'>",
        "purpose": "assessment",
        "type": "Activity type",
        "tagname": "Homework",
        "tagcolor": "#ff6b6b"
    }
}}
<div class="mimo-activity-card" data-activity-type="{{activitytype}}" data-testid="activity-card" role="button" tabindex="0">
    <div class="mimo-activity-card-content">
        {{#tagname}}
        <div class="mimo-activity-card-tag" data-testid="activity-tag" data-tag-name="{{tagname}}" style="background-color: {{tagcolor}};">
            {{tagname}}
        </div>
        {{/tagname}}
        <h3 class="mimo-activity-card-title">{{label}}</h3>
        <p class="mimo-activity-card-description">{{{description}}}</p>
        <div class="mimo-activity-card-icon activityiconcontainer {{purpose}} modicon_{{activitytype}}">
            {{{icon}}}
        </div>
    </div>
</div>
